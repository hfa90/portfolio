{
  "name": "clinicou",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:js": "node --check app.js",
    "test": "node --test",
    "verify": "npm run check:js && npm test",
    "supabase:migration:new": "supabase migration new",
    "supabase:db:push": "supabase db push",
    "supabase:advisors": "supabase db advisors"
  },
  "engines": {
    "node": ">=20"
  }
}
